
<%= form_with(model: position, local: true) do |form| %>
  <% if position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.select :all_val,  options %>
  </div>


  <div class="field">
    <%= form.label :user %>
    <% @users = User.all%>
    <% options = options_from_collection_for_select(@users, 'id', 'last_name') %>
    <%= form.select :all_val,  options %>
  </div>



<div>

</div>


  <div class="field">
    <%= form.label :fund_id %>
    <% @funds = Fund.all%>
    <% options = options_from_collection_for_select(@funds, 'id', 'name') %>
    <%= form.select :all_val,  options %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
